#pragma once

#include <cstddef>
#include <cstdint>
#include <string_view>

namespace crashbash::boot_image {

inline constexpr std::uint32_t kDiscLba = @CRASHBASH_BOOT_DISC_LBA@u;
inline constexpr std::uint32_t kSectorCount = @CRASHBASH_BOOT_SECTOR_COUNT@u;
inline constexpr std::size_t kPayloadBytes = @CRASHBASH_BOOT_PAYLOAD_BYTES@u;
inline constexpr std::string_view kSha256 = "@CRASHBASH_BOOT_SHA256@";
inline constexpr std::uint32_t kLoadAddress = @CRASHBASH_BOOT_LOAD_ADDRESS@u;
inline constexpr std::uint32_t kEntryPointerOffset = @CRASHBASH_BOOT_ENTRY_OFFSET@u;
inline constexpr std::uint32_t kEntry = @CRASHBASH_BOOT_ENTRY@u;

} // namespace crashbash::boot_image
