# Codemap

| Subsystem | Status | Where | Gap / next |
|---|---|---|---|
| Framework consumer | 🟡 loaded modules executing | `CMakeLists.txt`, `game/core/`, `external/psxport/`, `psxport.pin` | Pinned psxport d2266f4b executes BOOT and nested MENU with no recomp miss; the shared CDC lacks the oracle's pending-command phase machine, so GetTN is drained before resident poll state `0x8002DE2C` can observe it |
| C++ verification | ✅ enforced | `.clang-format`, `.clang-tidy`, `CMakeLists.txt`, `external/psxport/tools/check_cpp_style.py` | Normal CTest checks all first-party TUs for format, 1,200-line ownership, and compile-database clang-tidy; generated/framework code stays outside the first-party gate |
| Project tools | ✅ boot pipeline | `tools/run.py`, `tools/provision.py`, `tools/loaded_module.py`, `tools/recomp_bootstrap.py`, `tools/verify_boot.py`, `tools/verify_command_response_timing.py`, `tools/verify_oracle_irq.py`, `tools/verify_read_completion.py`, `tools/psxport_sync.py`, `tests/test_provision.py` | Default launcher owns sync→Clang build→provision→emit→product; executable/module identity, emitter, live-boundary, command-order, oracle-order, and read-completion checks each demonstrate both answers |
| Title integration | 🟡 BOOT + MENU substrate | `titles/crashbash/executable.json`, `titles/crashbash/boot_module.json`, `titles/crashbash/menu_module.json`, `game/recomp_seeds.json`, `generated/` (gitignored) | Real USA inputs emit 1,063 roots / 1,724 functions over resident, BOOT, and MENU ranges; full DAT and payload identities are tracked, while all extracted/generated bytes remain ignored |
| Native engine | 🟡 boot seam only | `game/core/crashbash_runtime.cpp`, `game/core/legacy_game_interface.h`, `game/core/recomp_register.cpp`, `game/core/main.cpp` | `CrashBashRuntime` owns boot dispatch through inheritance; measured config and an empty hook table remain bounded compatibility debt until generic framework algorithms stop reading `Core::cfg/Core::hooks`; no game behavior has been replaced |
| Native graphics producers | ⬜ missing | — | The native renderer initializes and both initial loaded modules execute, but command-response timing stops the resident CD state machine before any game frame; first fix that shared ordering defect, then RE camera/submitters from the loaded code |
| Widescreen | ⬜ missing | — | Blocked on native camera and producers |
| Interpolation | ⬜ missing | — | Blocked on PC ownership of transform producers |
| Differential harness | 🟡 CD ordering boundaries | `tools/verify_oracle_irq.py`, `tools/verify_read_completion.py`, `tools/verify_command_response_timing.py`, `tools/verify_boot.py`, `game/core/` | Independent Beetle proves read completion 1→0; the strict port comparator still rejects response/IRQ coalescing. The 136-line d2266f4b real-disc gate proves BOOT+MENU execution, and a bounded register trace proves handler-drain-before-poll ordering across 1,500 empty caller polls. Source-level oracle comparison additionally rejects the isolated delay-only candidate because it executes side effects early, undercounts parameter timing, coalesces INT3/INT2, and reverses same-deadline drive/command service. Full-memory lockstep remains unavailable |
| Project knowledge | ✅ maintained | `docs/codemap.md`, `docs/re-frontier.md`, `docs/issues/`, `docs/info/` | Registries record nested-range resolution, the GetTN timing boundary, loaded-module claim C011, current-framework blocker claim C012, and validated command-order instrument I008 |
