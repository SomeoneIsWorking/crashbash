# Codemap

| Subsystem | Status | Where | Gap / next |
|---|---|---|---|
| Framework consumer | 🟡 loaded modules executing | `CMakeLists.txt`, `game/core/`, `external/psxport/`, `psxport.pin` | Recorded psxport 17981527 descends from the landed oracle-derived CDC phase machine at 8611d756 with no CDC-source changes; the clean pinned runtime gate still awaits the sole execution slot |
| C++ verification | ✅ enforced | `.clang-format`, `.clang-tidy`, `CMakeLists.txt`, `external/psxport/tools/check_cpp_style.py` | Maintainer verification uses Clang format/tidy and the 1,200-line ownership gate; shipping CMake accepts the user's configured compiler without an identity whitelist or blacklist |
| Player launcher | ✅ current product | `run.sh`, `bootstrap.py`, `pyproject.toml`, `uv.lock`, `tests/test_bootstrap.py` | Zero arguments enter the frozen uv environment, provision and build `crashbash_port`, and open its window; the isolated player tree builds only that product with both CTest switches off, while `--check`, `--prepare-only`, and hermetic tests cover non-launching paths |
| Project tools | ✅ boot pipeline | `tools/provision.py`, `tools/loaded_module.py`, `tools/recomp_bootstrap.py`, `tools/verify_boot.py`, `tools/verify_command_response_timing.py`, `tools/verify_cdc_phase_progress.py`, `tools/verify_oracle_irq.py`, `tools/verify_read_completion.py`, `tools/psxport_sync.py`, `tests/test_provision.py` | Provisioning, executable/module identity, emitter, live-boundary, old command-order, positive CDC progress plus response-edge separation, oracle-order, and read-completion checks each demonstrate both answers |
| Title integration | 🟡 BOOT + MENU substrate | `titles/crashbash/executable.json`, `titles/crashbash/boot_module.json`, `titles/crashbash/menu_module.json`, `game/recomp_seeds.json`, `generated/` (gitignored) | Real USA inputs emit 1,063 roots / 1,724 functions over resident, BOOT, and MENU ranges; full DAT and payload identities are tracked, while all extracted/generated bytes remain ignored |
| Native engine | 🟡 boot seam only | `game/core/crashbash_runtime.cpp`, `game/core/legacy_game_interface.h`, `game/core/recomp_register.cpp`, `game/core/main.cpp` | `CrashBashRuntime` owns boot dispatch through inheritance; measured config and an empty hook table remain bounded compatibility debt until generic framework algorithms stop reading `Core::cfg/Core::hooks`; no game behavior has been replaced |
| Native graphics producers | ⬜ missing | — | The shared command-response defect is landed and its dirty candidate consumer crossed into later continuous reads, but no first game frame is proven; rerun the positive gate on the clean pinned product, then RE camera/submitters from the next real boundary |
| Widescreen | ⬜ missing | — | Blocked on native camera and producers |
| Interpolation | ⬜ missing | — | Blocked on PC ownership of transform producers |
| Differential harness | 🟡 CD ordering boundaries | `tools/verify_oracle_irq.py`, `tools/verify_read_completion.py`, `tools/verify_command_response_timing.py`, `tools/verify_cdc_phase_progress.py`, `tools/verify_boot.py`, `game/core/` | Independent Beetle proves read completion 1→0; the strict port comparator still requires that guest-visible result. The positive gate accepts the 8,606-line candidate trace with exact GetTN/command denominators, zero old polls, 306 sector events through LBA 17655, and 5/5 Pause INT3/INT2 pairs on distinct handler entries. Full-memory lockstep and a clean pinned positive/strict-completion run remain unavailable |
| Project knowledge | ✅ maintained | `docs/codemap.md`, `docs/re-frontier.md`, `docs/issues/`, `docs/info/` | Registries record nested-range resolution, the GetTN root cause and landed shared fix, loaded-module claim C011, phase-contract claim C013, response-edge claim C015, old-order instrument I008, and positive-progress instrument I009 |
